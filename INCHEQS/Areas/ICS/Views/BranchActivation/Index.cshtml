@using INCHEQS.TaskAssignment;
@using INCHEQS.Resources
@using (Html.BeginForm(null, null, FormMethod.Post, new { action = @Url.Action("SearchResultPage"), @Class = "form-inline secure-form submit-onload", id = "searchForm" }))
{
    Html.AntiForgeryToken();
    <h4>@ViewBag.SearchPage.PageTitle</h4>
    <div class="row">
        <hr />
        @Html.Partial("SearchPage/_GenericFieldsForm")
    </div>
    <hr />
    <div class="form-group">
        <button type="submit" class="btn btn-default" data-action="@Url.Action("SearchResultPage")">@Locale.Go</button>
    </div>

    <div id="resultContainer"></div>
}


@*using (Html.BeginForm(null, null, FormMethod.Post, new { action = @Url.Action("SaveMessage"), @Class = "form-inline secure-form", id = "message" }))
{
    <hr />
    <div id="resultContainer">
        <table>
            <tr>
                <td class="TextBox">Please key in your message</td>
            </tr>
            <tr>
                <td>
                    <textarea maxlength="512" name="txtMessage" id="txtMessage" value=""></textarea>
                </td>
            </tr>
            <tr>
                <td class="TextBox">or select one message</td>
            </tr>
            <tr>
                <td>
                    <select name="MessageOption" id="MessageOption" style="width:100% ">
                        <option value="">--Select--</option>
                        @foreach (var row in ViewBag.Message.Rows)
                        {
                            <option value="@row["fldBroadcastMessage"]">@row["fldBroadcastMessage"]</option>
                        }
                    </select>

                </td>
            </tr>
        </table>
        <br>
        <button type="submit" class="btn btn-default" data-action="@Url.Action("SaveMessage")">@Locale.btnOK</button>
    </div>
}*@
@Scripts.Render("~/bundles/SearchPage")