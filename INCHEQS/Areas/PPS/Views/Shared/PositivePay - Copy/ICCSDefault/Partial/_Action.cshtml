@using INCHEQS.Resources
<div class="row">
    <div class="col-sm-3">
        @*@if (ViewBag.TaskId != "308250" && ViewBag.TaskId != "308260")
        {
            if (ViewBag.TaskId == "308160")
            {
                <button type="button" id="approveBtn" data-action="@Url.Action("BranchApprove")" class="btn btn-default btn-xs normal-submit">@Locale.Approve</button>
                <button type="button" id="returnBtn" data-action="@Url.Action("BranchReturn")" class="btn btn-default btn-xs normal-submit">@Locale.Return</button>
            }
            else
            {
                //if (ViewBag.TaskId == "318190" || ViewBag.TaskId == "318200" || ViewBag.TaskId == "318350" || ViewBag.TaskId == "318360")
                //{
                <button type="button" id="approveBtn" data-action="@Url.Action("VerificationApprove")" class="btn btn-default btn-xs normal-submit">@Locale.Approve</button>
                <button type="button" id="returnBtn" data-action="@Url.Action("VerificationReturn")" class="btn btn-default btn-xs normal-submit">@Locale.Return</button>

                @*<button type="button" class="btn btn-default btn-xs modal-submit" data-action="@Url.Action("PullOutReason")">@Locale.PullOut</button>

                //}
            }
        }*@
        @if (ViewHelper.HasUrlAccess(CurrentUser.Account, ViewBag.AllowedAction, "VerificationApprove"))
        {
            <button type="button" id="approveBtn" data-action="@Url.Action("VerificationApprove")" class="btn btn-default btn-xs normal-submit">@Locale.Approve</button>
        }
        @if (ViewHelper.HasUrlAccess(CurrentUser.Account, ViewBag.AllowedAction, "VerificationReturn"))
        {
            <button type="button" id="returnBtn" data-action="@Url.Action("VerificationReturn")" class="btn btn-default btn-xs normal-submit">@Locale.Return</button>
        }
        @if (ViewHelper.HasUrlAccess(CurrentUser.Account, ViewBag.AllowedAction, "VerificationRoute"))
        {
            <button type="button" id="routeBtn" data-action="@Url.Action("VerificationRoute")" class="btn btn-default btn-xs normal-submit">@Locale.Route</button>
        }
        @if (ViewHelper.HasUrlAccess(CurrentUser.Account, ViewBag.AllowedAction, "VerificationPullOut"))
        {
            @*<button type="submit" id="pullOutBtn" data-action="@Url.Action("PullOutReason")" class="btn btn-default btn-xs modal-submit">@Locale.PullOut</button>*@
            <a class="btn btn-default btn-xs modal-submit" id="pulloutReasonBtn" data-action="@Url.Action("PullOutReason")">@Locale.PullOut</a>


        }
    </div>
    <div class="col-sm-3">
        
            <input type="text" style="width:30px;min-width:30px" id="rejectCodeText" name="new_textRejectCode" value="@ViewBag.InwardItemViewModel.getField("fldRejectCode").Trim()" />
            <select class="textbox" id="rejectCodeSelectList" name="new_rejectCode" style="width:240px;min-width:240px">
                <option value="">--- @Locale.SelectReturnRouteReason ---</option>
            </select>
        
    </div>

    <div class="col-sm-8">
        
        <textarea class="remarks" name="textAreaRemarks" placeholder="@ViewBag.InwardItemViewModel.getField("fldRemarks")" value="@ViewBag.ChequeInfo.fldRejectCode"></textarea>
        <input type="hidden" id="remarkField" name="remarkField" value="">
    </div>

    <div class="col-sm-2">
        Service Charge <input type="text" name="fldCharges" style="width:40px;min-width:40px" id="serviceCharge" value="@ViewBag.InwardItemViewModel.getField("fldCharges")" />
    </div>
</div>
